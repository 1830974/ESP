<Page x:Class="Administration.Views.ReportPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:Administration.Views"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="1024"
      Title="Rapport">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Page Rapport" FontSize="24" FontWeight="Bold" Margin="0 0 0 20"/>

        <ScrollViewer Grid.Row="1">
            <StackPanel>

                <materialDesign:Card Margin="0 0 0 20" Padding="20">
                    <StackPanel>
                        <TextBlock Text="1. Rapport de revenu" FontSize="20" FontWeight="Bold" Margin="0 0 0 10"/>
                        <TextBlock Text="Ce rapport présente une analyse complète de vos revenus." TextWrapping="Wrap" Margin="0 0 0 10"/>

                        <DatePicker materialDesign:HintAssist.Hint="Date de début" Margin="0 5 0 5"/>

                        <DatePicker materialDesign:HintAssist.Hint="Date de fin" Margin="0 5 0 5"/>

                        <Button Content="Générer le rapport de revenu" Style="{StaticResource MaterialDesignRaisedButton}" Margin="0 10 0 0"/>
                    </StackPanel>
                </materialDesign:Card>

                <materialDesign:Card Margin="0 0 0 20" Padding="20">
                    <StackPanel>
                        <TextBlock Text="2. Rapport de debug" FontSize="20" FontWeight="Bold" Margin="0 0 0 10"/>
                        <TextBlock Text="Le rapport de debug permet d'identifier et résoudre les problèmes techniques." TextWrapping="Wrap" Margin="0 0 0 10"/>

                        <xctk:DateTimePicker Value="{Binding DebugReportStartDate}" materialDesign:HintAssist.Hint="Date et heure de début" Margin="0 5 0 5" />

                        <xctk:DateTimePicker Value="{Binding DebugReportEndDate}" materialDesign:HintAssist.Hint="Date et heure de fin" Margin="0 5 0 5" />

                        <Button Content="Voir le rapport de debug" Style="{StaticResource MaterialDesignRaisedButton}" Margin="0 10 0 0"/>

                        <TextBlock Text="Détails des requêtes bloquées:" FontWeight="Bold" Margin="0 20 0 10"/>
                        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding BlockedRequests}" Height="200">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID de requête" Binding="{Binding RequestId}"/>
                                <DataGridTextColumn Header="Utilisateurs" Binding="{Binding Users}"/>
                                <DataGridTextColumn Header="Raison" Binding="{Binding Reason}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </materialDesign:Card>

                <materialDesign:Card Margin="0 0 0 20" Padding="20">
                    <StackPanel>
                        <TextBlock Text="3. Rapport des logs" FontSize="20" FontWeight="Bold" Margin="0 0 0 10"/>
                        <TextBlock Text="Le rapport des logs vous permet de suivre toutes les activités du système." TextWrapping="Wrap" Margin="0 0 0 10"/>

                        <DatePicker materialDesign:HintAssist.Hint="Date de début" Margin="0 5 0 5"/>

                        <DatePicker materialDesign:HintAssist.Hint="Date de fin" Margin="0 5 0 5"/>

                        <TextBox materialDesign:HintAssist.Hint="Type d'entrée" Margin="0 5 0 5"/>

                        <Button Content="Afficher le rapport des logs" Style="{StaticResource MaterialDesignRaisedButton}" Margin="0 10 0 0"/>
                    </StackPanel>
                </materialDesign:Card>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
