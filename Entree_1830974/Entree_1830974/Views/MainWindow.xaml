<Window x:Class="Entree_1830974.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:Entree_1830974.Views"
        mc:Ignorable="d"
        Title="MainWindow" Height="640" Width="480">

    <Window.InputBindings>
        <KeyBinding Key="Return" Command="{Binding EnterKeyPressedCommand}" />
    </Window.InputBindings>

    <Grid Background="#FF4CAF50">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="{Binding DisplayMessage}" 
                       FontSize="36" 
                       FontWeight="Bold" 
                       Foreground="White" 
                       TextAlignment="Center" 
                       TextWrapping="Wrap"
                       Margin="0,0,0,20"/>
        </StackPanel>
    </Grid>
</Window>
